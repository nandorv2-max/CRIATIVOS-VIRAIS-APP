// This component has been deprecated and its functionality has been merged into
// the more comprehensive ProjectBrowserModal.tsx to centralize project loading logic.
// Keeping the file to avoid breaking imports, but it no longer renders anything.
import React from 'react';

const MyProjectsModal: React.FC = () => {
  return null;
};

export default MyProjectsModal;